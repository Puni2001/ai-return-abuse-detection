{
  "name": "Lint Python Files on Save",
  "version": "1.0.0",
  "description": "Automatically run linting checks when Python files are saved",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.py"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Check the edited Python file for code quality issues using flake8 or pylint standards. Fix any issues found."
  }
}
